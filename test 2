# Write a function called fizz buzz
# it takes a number. 
# 1. If the number is divisble by 2, it should return "Fizz"
# 2. If it divisible by 5, it should return fizz
# 3. If it is divisible by both (3 & 5)  it should return fizzbuzz.
# 4. Otherwise it should return the same number. 

def fizz_buzz():
    number = int(input("Enter number: "))

    if number % 3 == 0 and number % 5 == 0:
        print("FizzBuzz") 
    elif number % 5 == 0:
        print("Buzz") 
    elif number % 3 == 0:
        print("Fizz") 
    else:
        return number
    
def main():
    fizz_buzz()

main()
