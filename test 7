# Write a function that prints all the prime
# numbers between 0 and limit where limit 
# is a parameter. 

limit = int(input("Enter the limit of the prime numbers: "))

for num in range(1, limit):
    for i in range(2, num):
        if num % i == 0:
            break
        else:
            print(num)
            break
